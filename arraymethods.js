//push
//pop
//shift
//unshift
//splice
//slice

//////////////
  
const arr=[5,4,6,7,8,2,9];
const out=arr.push(1);//inserting a element in last
const out1=arr.pop(6);//delecting last element of an array
const out2=arr.shift();//delecting first element in an array
const out3=arr.unshift(1,3);//adding the values in front of the array
const  out4=arr.splice(1,1);//removing one element only remaing elements are showing
const out5=arr.slice(1,4)


console.log(arr);

